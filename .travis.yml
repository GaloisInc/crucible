script:
- cabal update
- cabal install hlint
- hlint crucible{,-jvm,-llvm,-saw,-server,-syntax} crux{,-llvm} what4{,-abc,-blt}
- cabal new-build crucible{,-jvm,-llvm,-saw,-syntax} crux{,-llvm} what4{,-abc,-blt}
  -j --disable-optimization $BUILD_ARG
before_cache:
- rm -fv $HOME/.cabal/packages/hackage.haskell.org/build-reports.log
- rm -fv $HOME/.cabal/packages/hackage.haskell.org/00-index.tar
matrix:
  fast_finish: true
  include:
  - env: CABALVER=2.4 GHCVER=8.6.3 BUILD_ARG=
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-2.4
        - ghc-8.6.3
        - libglpk-dev
        - libntl-dev
        - libboost-all-dev
  - env: CABALVER=2.4 GHCVER=8.4.3 BUILD_ARG=
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-2.4
        - ghc-8.4.3
        - libglpk-dev
        - libntl-dev
        - libboost-all-dev
  - env: CABALVER=2.4 GHCVER=8.6.3 BUILD_ARG=
    addons:
      homebrew:
        packages:
        - ghc
        - ntl
        - glpk
        - cabal-install
        update: true
    os: osx
  - env: CABALVER=2.4 GHCVER=8.6.3 BUILD_ARG=--allow-newer
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - cabal-install-2.4
        - ghc-8.6.3
        - libglpk-dev
        - libntl-dev
        - libboost-all-dev
  allow_failures:
  - env: CABALVER=2.4 GHCVER=8.6.3 BUILD_ARG=--allow-newer
cache:
  directories:
  - $HOME/.cabsnap
  - $HOME/.cabal/packages
  - $HOME/.ghc
before_install:
- unset CC
- export PATH=/opt/ghc/$GHCVER/bin:/opt/cabal/$CABALVER/bin:$PATH
dist: xenial
git:
  submodules: true
language: c
sudo: false
# This file was auto-generated by travis/gen.sh. DO NOT EDIT.
