// FIXME: Toplevel bindings in Cryptol context
and_assoc = {{ \(x, y, z : Bit) -> ((x && y) && z) == (x && (y && z)) }};

main = do {
  result <- prove abc {{ \(x, y, z : Bit) -> ((x && y) && z) == (x && (y && z)) }};
  print result;
};
