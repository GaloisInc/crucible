main = do {
  m <- cryptol_module "mutRecTest.cry";
  (f: [8] -> [32]) <- cryptol_extract m "streamFibs";
  (g: [8] -> [32]) <- cryptol_extract m "streamFibsAlt";
  (h: [8] -> [32]) <- cryptol_extract m "recFibs";
  (i: [8] -> [32]) <- cryptol_extract m "recFibs2";

  let thm1 x = f x == g x;
  prove_print abc thm1;

  // FIXME, this makes the simulator crash the stack
  //let thm2 x = f x == h x;
  // prove_print abc thm2;

};
