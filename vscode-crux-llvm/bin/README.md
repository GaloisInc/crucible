This directory can be used to put symbolic link to the necessary executables to
run the extension for developers of the extension.  Notably, the nix shell will
use it that way, so as to not pollute the toplevel directory with many symbolic
links.
