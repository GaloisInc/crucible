{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "problemMatcher": [
                "$tsc"
            ],
            "group": "build",
            "label": "Nix-aware build", 
            "command": "npx tsc -b",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}