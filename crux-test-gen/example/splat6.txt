// `splat`'s nested expansion respects constraints that are added after the
// `splat` itself.
start ::= <<with_var>>

for[T] with_var ::= <<splat[x[T]]>> <<x[T]>>

x[A] ::= A
x[B] ::= B

// output 0: A A
// output 1: B B
