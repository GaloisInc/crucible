(defun @test () Unit
   (start block:
      (let r (fresh Real))
      (let f (real-to-fp Float invalid-mode r))
      (return ())))

At test-data/ux/invalid-rounding-mode.cbl:5:30, expected rna or rne or rounding mode or rtn or rtp or rtz but got invalid-mode
