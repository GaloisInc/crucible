(defun @test () Integer
   (start block:
      (let x (the (Variant Integer Bool) (inj 0 (the Integer 1))))
      (case x a:))
   (defblock (a: v Integer) (return v)))

At test-data/ux/case-wrong-arity.cbl:6:7, expected known label with an BoolRepr argument but got a:
